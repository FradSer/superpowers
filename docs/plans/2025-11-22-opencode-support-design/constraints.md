# Constraints

- OpenCode plugin APIs and lifecycle hooks determine integration boundaries.
- Skill discovery should share logic with existing Codex implementation where possible.
- Avoid regressions in existing skill naming and priority semantics.
- Keep bootstrap behavior deterministic and low-friction.
